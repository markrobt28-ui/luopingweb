# Database - 本地 PostgreSQL 配置
# PostgreSQL 安装路径: H:\Program Files\PostgreSQL
# 请确保本地已安装并启动 PostgreSQL 服务
# 默认端口: 5432, 默认用户: postgres, 请根据实际情况修改密码
DATABASE_URL="postgresql://postgres:password@localhost:5432/luoping_web?schema=public"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-min-32-chars"
JWT_EXPIRES_IN="7d"

# Redis - 本地 Redis 配置
# 请确保本地已安装并启动 Redis 服务
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_PASSWORD=""
REDIS_DB=0
REDIS_ENABLED="true"

# App
PORT=3000
NODE_ENV="development"

# CORS - 生产环境允许的源（逗号分隔）
ALLOWED_ORIGINS="http://localhost:5173,http://localhost:5174"
